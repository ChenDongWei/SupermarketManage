<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java1234.dao.OrderDao">

	<resultMap type="Order" id="OrderResult">
		<result property="id" column="id"/>
		<result property="billCode" column="billCode"/>
		<result property="productName" column="productName"/>
		<result property="totalPrice" column="totalPrice"/>
		<result property="creationDate" column="creationDate"/>
		<association property="customer" column="providerId" select="com.java1234.dao.CustomerDao.findById"></association>
	</resultMap>
	
	<select id="find" parameterType="Map" resultMap="OrderResult">
		select * from t_bill
		<where>
			<if test="providerId!=null and providerId!='' ">
				and providerId = #{providerId}
			</if>
		</where>
		<if test="start!=null and size!=null">
			limit #{start},#{size}
		</if>
	</select>

	
	<select id="getTotal" parameterType="Map" resultType="Long">
		select count(*) from t_bill
		<where>
			<if test="providerId!=null and providerId!='' ">
				and providerId = #{providerId}
			</if>
		</where>
	</select>
	
	<select id="findById" parameterType="Integer" resultMap="OrderResult">
		select * from t_bill where id=#{id}
	</select>
	
	<select id="findLastOrderByCusId" parameterType="Integer" resultMap="OrderResult">
		SELECT * FROM t_bill WHERE providerId=#{providerId} ORDER BY orderdate DESC LIMIT 0,1;
	</select>
</mapper> 